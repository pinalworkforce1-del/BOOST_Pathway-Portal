name: Route Pinal Rosie v17.1

on:
  push:
    paths:
      - '.github/workflows/route-rosie-v171.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  route:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Route Rosie endpoint
        shell: python
        run: |
          from pathlib import Path
          p = Path('assets/js/pinal-ask-rosie-v1.js')
          old = "https://dxcajwarqojvmbteroco.supabase.co/functions/v1/boost-ask-rosie"
          new = "https://dxcajwarqojvmbteroco.supabase.co/functions/v1/boost-ask-rosie-v171"
          s = p.read_text()
          if new in s:
              print('Already routed to v17.1')
          elif old in s:
              p.write_text(s.replace(old, new, 1))
              print('Routed Pinal Rosie to v17.1')
          else:
              raise SystemExit('Expected Pinal Rosie endpoint not found; no file changed.')
      - name: Commit route change
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/js/pinal-ask-rosie-v1.js
          git commit -m "Route Pinal Rosie through v17.1 safety wrapper"
          git push
